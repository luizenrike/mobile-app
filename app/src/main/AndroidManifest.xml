<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SwiftShop"
        tools:targetApi="31">
        <activity
            android:name=".activities.Pedidos.Pedidos"
            android:exported="false" />
        <activity
            android:name=".activities.Pagamento.Pagamento"
            android:exported="false" />
        <activity
            android:name=".activities.DetalhesProduto.DetalhesProduto"
            android:exported="false" />
        <activity
            android:name=".activities.TelaPrincipal.TelaPrincipal"
            android:exported="false" />
        <activity
            android:name=".activities.FormCadastro.FormCadastro"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.FormLogin.FormLogin"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>